---
import Counter from '../components/Counter.svelte';
import Card from '../components/Card.svelte';
import ResponsiveGrid from '../components/ResponsiveGrid.svelte';
import '../styles/global.css';

const sampleItems = [
{
    id: 1,
    title: "TailwindCSS",
    description: "ユーティリティファーストのCSSフレームワーク"
  },
  {
    id: 2,
    title: "Svelte 5",
    description: "新しいリアクティビティシステム「Runes」を採用"
  },
  {
    id: 3,
    title: "Astro",
    description: "モダンな静的サイトジェネレーター"
  }
];
---
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>TailwindCSSデモ</title>
    <script is:inline src="/scripts/theme.js"></script>
  </head>
  <body>
    <main class="container-custom py-8 space-y-12">
      <header class="text-center mb-12">
        <h1 class="heading-1 mb-4">TailwindCSS設計</h1>
        <p class="body-large">data-theme属性ベースのダークモードとセマンティックカラーシステム</p>
      </header>
      
      <section class="max-w-md mx-auto">
        <h2 class="heading-2 mb-4 text-center">Counter</h2>
        <Counter client:load />
      </section>

      <section>
        <h2 class="heading-2 mb-6 text-center">Card</h2>
        <div class="max-w-sm mx-auto">
          <Card 
            title="TailwindCSS" 
            description="@layer ディレクティブによる適切な層分離、data-theme属性ベースのダークモード、セマンティックカラーシステムを実装しました。"
            buttonText="詳細を確認"
            client:load 
          />
        </div>
      </section>

      <section>
        <h2 class="heading-2 mb-6 text-center">Responsive Grid</h2>
        <ResponsiveGrid items={sampleItems} client:load />
      </section>

      <section class="text-center">
        <div class="card max-w-md mx-auto p-6">
          <h2 class="heading-3 mb-3">システムテーマ自動検出</h2>
          <p class="body-text mb-3">
            OSの設定に応じて、リアルタイムでテーマが自動切り替えされます。
          </p>
          <div class="badge badge-primary">
            data-theme属性ベース
          </div>
        </div>
      </section>
    </main>
  </body>
</html>
